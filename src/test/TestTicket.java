package test;

import static org.junit.Assert.*;

import java.util.ArrayList;
import java.util.Date;

import model.Constants;
import model.Passageiro;
import model.Percurso;
import model.Reserva;

import org.junit.Before;
import org.junit.Test;

import controller.PassageiroController;
import controller.ReservaController;
import controller.TicketController;

public class TestTicket {

    int temp_id_reserva, temp_id_percurso, temp_id_modal, temp_id_usuario, temp_id_viagem;
    String[] temp_ids_passageiros, temp_ids_percursos;

    @Before
    public void init() {
    }

    @Test
    public void cadastrarTicket() {
        try {
        } catch (Exception e) {
            // TODO: handle exception
        } finally {

        }
    }

}
